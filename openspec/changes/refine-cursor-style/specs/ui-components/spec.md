# Cursor Style Spec

## Requirements

### Requirement: Pointer Cursor

All interactive elements, specifically `Select` triggers/items and `Combobox` triggers/items, MUST display a `pointer` cursor (hand icon) when hovered.

### Requirement: Consistency

This behavior MUST be consistent across all instances of these components.
